import {
    makeStyles
} from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    actionDiv: {
        textAlign: "center",
    },
    mainContainer: {
        alignItems: "center",
        display: "flex",
    },
    smMargin: {
        margin: theme.spacing(1),
    },
}));